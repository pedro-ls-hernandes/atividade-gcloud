## Rodando o projeto localmente

```bash
npm run dev

npm build

npm start
```

## Para criar o container da aplicação

```bash
docker build -t atividade-app-cloud .

docker run -p 3000:3000 atividade-app-cloud
```
> Acesse via Local ou Network no terminal
